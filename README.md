# Client-side-real-time-object-detection
Real-time object detection app built with Angular and TensorFlow.js. It runs the COCO-SSD model directly in the browser to detect 80+ objects via webcam. Features GPU acceleration and a decoupled render loop for smooth 60 FPS performance without backend latency.
